#include <iostream>
using namespace std;
int main(){
    int x, y;
    cin>>x>>y;
    int a, b;
    char w;
    string s;
    while(cin>>a>>b>>w){
        getline(sin, s);
        switch (w){
            case 'N':
                for(int i=0; i<s.size(); i++){
                    switch(s[i]){
                        case 'R':
                        
                        case 'L':
                        case 'F':
                    }
                }
            case 'E':
                for(int i=0; i<s.size(); i++){
                    switch(s[i]){
                        case 'R':

                        case 'L':
                        case 'F':
                    }
                }
            case 'S':
                for(int i=0; i<s.size(); i++){
                    switch(s[i]){
                        case 'R':

                        case 'L':
                        case 'F':
                    }
                }
            case 'W':
                for(int i=0; i<s.size(); i++){
                    switch(s[i]){
                        case 'R':

                        case 'L':
                        case 'F':
                    }
                }
        }
        
    }
    return 0;
}